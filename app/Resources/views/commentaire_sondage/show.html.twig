{% extends 'base.html.twig' %}

{% block body %}
    <h1>Commentaire_sondage</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_Sondage.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire_Sondage.commentaire }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if commentaire_Sondage.date %}{{ commentaire_Sondage.date|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('commentaire_sondage_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('commentaire_sondage_edit', { 'id': commentaire_Sondage.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
