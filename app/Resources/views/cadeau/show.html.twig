{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cadeau</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cadeau.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ cadeau.intitule }}</td>
            </tr>
            <tr>
                <th>Urlimg</th>
                <td>{{ cadeau.urlImg }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('cadeau_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('cadeau_edit', { 'id': cadeau.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
