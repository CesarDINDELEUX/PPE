{% extends 'base.html.twig' %}

{% block body %}
    <h1>Question_quizz</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question_Quizz.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ question_Quizz.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('question_quizz_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('question_quizz_edit', { 'id': question_Quizz.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
