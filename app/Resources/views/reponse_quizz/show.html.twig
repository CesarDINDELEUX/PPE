{% extends 'base.html.twig' %}

{% block body %}
    <h1>Reponse_quizz</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse_Quizz.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ reponse_Quizz.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('reponse_quizz_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('reponse_quizz_edit', { 'id': reponse_Quizz.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
