{% extends 'base.html.twig' %}

{% block body %}
    <h1>Quizz</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quizz.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ quizz.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('quizz_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('quizz_edit', { 'id': quizz.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
