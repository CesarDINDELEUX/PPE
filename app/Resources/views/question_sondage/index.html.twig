{% extends 'base.html.twig' %}

{% block body %}
    <h1>Question_sondages list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Intitule</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for question_Sondage in question_Sondages %}
            <tr>
                <td><a href="{{ path('question_sondage_show', { 'id': question_Sondage.id }) }}">{{ question_Sondage.id }}</a></td>
                <td>{{ question_Sondage.intitule }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('question_sondage_show', { 'id': question_Sondage.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('question_sondage_edit', { 'id': question_Sondage.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('question_sondage_new') }}">Create a new question_Sondage</a>
        </li>
    </ul>
{% endblock %}
