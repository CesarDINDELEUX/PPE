{% extends 'base.html.twig' %}

{% block body %}
    <h1>Question_sondage</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ question_Sondage.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ question_Sondage.intitule }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('question_sondage_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('question_sondage_edit', { 'id': question_Sondage.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
